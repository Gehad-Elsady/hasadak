import 'package:flutter/material.dart';

class Engineers extends StatelessWidget {
  const Engineers({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      child: Center(child: Text('Engineers')),
    );
  }
}
